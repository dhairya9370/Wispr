<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Wispr</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; font-family: Arial, sans-serif; }
        body { 
             min-width: 450px;
            display: flex; 
            justify-content: center; 
            align-items: center; 
            height:auto; 
            background-color: rgb(255, 251, 234);
        }
        .container { 
            width: 800px;
            min-width: 400px;
            background: rgba(255, 255, 255, 0.98);
            padding: 30px;
            border-radius: 20px;
            text-align: center;
            border:solid black 1px;
            margin:30px;
        }
        /* #auth-container */
        .logo {
            width: 50px;
            height: 50px;
            margin-bottom: 15px;
        }
        h2 { margin-bottom: 20px; color: #128C7E; font-size: 26px; font-weight: bold; }
        input { 
            width: 350px;
            padding: 15px;
            padding-left: 23px;;
            margin: 5px 0;
            border: 1px solid black;
            border-radius: 150px;
            font-size: 15px;
            
            outline: none;
            transition: 0.3s;
        }
        input:focus {
            
            border:solid rgb(65, 104, 36) 1px;
        }
        button { 
            width: 90px;
            padding: 10px;
            background: #19bc55;
            color: white;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-size: 16px;
            font-weight:600;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            transition: 0.3s;
            outline: none;
            margin-top: 30px;
        }
        button:hover { background: #0f813c; }
        .switch-text {
            margin-top: 15px;
            font-size: 14px;margin-bottom: 25px;
        }
        .switch-text a {
            color: #128C7E;
            text-decoration: none;
            font-weight: bold;
            cursor: pointer;
            
        }
        
    </style>
</head>
<body>
    <div class="container" id="auth-container">
        <div id="login-box">
            <h2>Wispr</h2>
            <form method="POST" action="http://localhost:3000/wispr/login" >
                <input type="text" name="btId" placeholder="BT ID" required><br>
                <input type="password" name="pass" placeholder="Password" required><br>
                <button type="submit">Login</button>
            </form>
            <p class="switch-text">New user? <a href="http://localhost:3000/wispr/register">Register here</a></p>
        </div>


    <!-- <script>
        // const socket = new WebSocket("ws://localhost:3000");
    </script> -->
</body>
</html>
